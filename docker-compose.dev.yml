version: '3'

services:
  webserver:
    image: nginx
    container_name: webserver
    volumes:
     - ./frontend/:/usr/share/nginx/html
    ports:
     - 8080:80
    networks:
     - app-network
  server:
    build: ./backend
    ports:
      - 3000:3000
    volumes:
      - ./backend:/app
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
